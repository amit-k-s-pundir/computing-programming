create table document (
       name text,
       path text,
       type text,	-- (or directory? file? etc?)
       open-with text,
       tag-list text,
       registeration-date date,
       note text);




