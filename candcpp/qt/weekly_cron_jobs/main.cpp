#include <iostream>
#include <vector>
#include <string>
#include <iostream>
#include <initializer_list>
#include <algorithm>

using namespace std;

template<class A> class Build{
  void make(A::BuildOptions options){
          string clean_cmd, config_cmd, build-cmd, install-cmd, cmd;
          bool autogen_p;
    for(auto i = options.config_options.begin(); i != options.config_options.end();

  }
};

class Emacs{

int main()
{
  Build<Emacs>::make(Emacs::BuildOptions buildOptions);
  cout << "Hello World!" << endl;
  return 0;
}

