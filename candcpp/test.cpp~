#include <stdio.h>

int global_var = 3;

int main(){
	printf("Address of global_var is 0x%x\n", &global_var);
	printf(	"Sleeping infinitely; my pid is %d\n", getpid());
	while(1)
		sleep(5);
	return 0;
}
