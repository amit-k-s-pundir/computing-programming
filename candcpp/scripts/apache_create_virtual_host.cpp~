#include <iostream>
#include <string>

#include "boost/filesystem.hpp"

namespace fs = boost::filesystem;
using namespace std;
using namespace fs;

void createVirtualHost(const string& sitesAvailableDir = "/etc/apache2/sites-available",
                const string& defaultSite = "default", const string& siteBaseDir = "/home/sonu/workspace/www",
                const string& siteName){

}

int main(){
}
